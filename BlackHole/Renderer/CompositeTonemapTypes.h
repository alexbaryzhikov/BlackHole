#import "SharedTypes.h"

typedef enum {
    CompositeTonemapTextureIndexOutput,
    CompositeTonemapTextureIndexHDR,
    CompositeTonemapTextureIndexBloom,
} CompositeTonemapTextureIndex;

typedef enum {
    CompositeTonemapBufferIndexEDRHeadroom,
} CompositeTonemapBufferIndex;
